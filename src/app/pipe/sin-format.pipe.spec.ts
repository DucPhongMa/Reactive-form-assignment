import { SinFormatPipe } from './sin-format.pipe';

describe('SinFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SinFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
